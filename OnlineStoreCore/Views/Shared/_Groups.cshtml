@using OnlineStoreCore.Data.Repositories

@inject IGroupRepository _groupRepository;

<li class="nav-item dropdown">
    <a class="nav-link dropdown-toggle" href="#" id="dropdown08" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">گروه ها</a>
    <div class="dropdown-menu" aria-labelledby="dropdown08">
        @foreach (var item in _groupRepository.GetAllCategories())
        {
            <a class="dropdown-item" href="/Group/@item.Id/@item.Name">@item.Name</a>
        }
    </div>
</li>